<h1>Listing wears</h1>

<table>
  <tr>
    <th>Date</th>
    <th>Garment</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @wears.each do |wear| %>
  <tr>
    <td><%= wear.date %></td>
    <td><%= wear.garment_id %></td>
    <td><%= wear.user_id %></td>
    <td><%= link_to 'Show', wear %></td>
    <td><%= link_to 'Edit', edit_wear_path(wear) %></td>
    <td><%= link_to 'Destroy', wear, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Wear', new_wear_path %>
