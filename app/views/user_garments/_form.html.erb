<%= form_for(@user_garment) do |f| %>
  <% if @user_garment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_garment.errors.count, "error") %> prohibited this user_garment from being saved:</h2>

      <ul>
      <% @user_garment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :garment_id %><br />
    <%= f.number_field :garment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
